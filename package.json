{
  "name": "logScheduler",
  "private": false,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "web-vitals": "^3.4.0"
  }
}
